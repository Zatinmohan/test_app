import 'package:flutter/material.dart';

///  ********************************************************************
///   Contains each and every color that has been used in app
///   Useful for maintaining the same colors for dark theme as well or for custom themes as well.
///  ********************************************************************

class ColorConstants {
  static const Color kPrimaryColor = Color(0xffF5862A);
  static const Color kPrimaryBackgroundColor = Color(0xffFFFFFF);
  static const Color kPrimaryButtonColor = Color(0xffF5862A);
  static const Color kPrimaryTextColor = Color(0xff000000);
}
